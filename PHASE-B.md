# Phase B — Create Order UCIC fields + Dashboard

## Done
- **Create Order** form: Coupon, ERP Order #, Delivery Area, Driver, Vehicle + live dealers/products
- **Backend Order**: stores `CouponNumber`, `ErpOrderNumber`, `DeliveryArea`, `Driver`, `Vehicle`
- **Dashboard**: recent orders show Coupon, Status, Area, Driver, Vehicle, ERP from live API
- **Orders list / Order detail**: same UCIC fields mapped from API

## Client note
UI changes need a **new APK** (or Phase E OTA). Data already works once API is redeployed.

## Next
Phase D — error / mock fallback cleanup  
Phase E — OTA + final APK
